ERPNEXT_VERSION=v16.17.0
DB_PASSWORD=changeit

# Site name used by frappe to resolve requests.
# Replace with the final host or with a fixed local site name.
FRAPPE_SITE_NAME_HEADER=boutique.local

# Local Linux deployment defaults.
HTTP_PUBLISH_PORT=8080

# Uncomment only after you have built the custom image with ./bootstrap build.
# CUSTOM_IMAGE=custom
# CUSTOM_TAG=16
# PULL_POLICY=missing
